<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/codehubn"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddContest"></activity>
        <activity android:name=".title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotificationActivity" />
        <activity android:name=".websites" />
        <activity android:name=".sites" />
        <activity android:name=".alterlayout" />
        <activity android:name=".contests" />
        <activity android:name=".suggestion" />
        <activity android:name=".credit" />
        <activity android:name=".Login"></activity>
        <activity android:name=".Register" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="AlarmReciever.messaging.id"
            android:value="@string/id" />

        <receiver android:name=".AlarmReciever">
            <intent-filter>
                <action android:name="s.aj.action.DISPLAY_NOTIFICATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>